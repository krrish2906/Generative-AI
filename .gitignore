# Virtual environment (present in root)
venv/

# Environment variables (API keys)
.env

# Python cache (generated in all folders)
**pycache**/
*.pyc

# Streamlit cache (if used later)
.streamlit/

# HuggingFace / model cache (if models downloaded)
.cache/

# OS-generated files
Thumbs.db
.DS_Store